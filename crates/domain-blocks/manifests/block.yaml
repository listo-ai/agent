id: sys.agent.block
display_name: Block
# `isContainer` lets block nodes live under `sys.core.folder` / station
# without amending those manifests, and accommodates the Stage 3b/3c
# evolution (blocks holding `.process` / `.wasm` / `.native` children).
facets:
  - isSystem
  - isContainer
containment:
  must_live_under: []
  may_contain: []
  cardinality_per_parent: many_per_parent
  cascade: strict
slots:
  - name: lifecycle
    role: status
    value_schema:
      type: string
      enum: ["discovered", "validated", "enabled", "disabled", "failed"]
  - name: version
    role: status
    value_schema:
      type: string
  - name: last_error
    role: status
    value_schema:
      type: string
  - name: enabled
    role: config
    writable: true
    value_schema:
      type: boolean
      default: true
